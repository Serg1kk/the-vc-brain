# Post-MVP Parking Lot

Ideas explicitly deferred past the 24h MVP. Source: roadmap «Post-MVP» section + feature
READMEs' Boundaries blocks.

- **Voice interview agent (ElevenLabs conversational) — FIRST stretch, in-hackathon if time
  remains after MVP** (operator, Jul 19): a mode choice at intake — text chat OR a voice agent
  talking from the site, personalized by the founder's card, with tools. Operator's estimate:
  relatively quick on ElevenLabs. Longer-term version also hears intonation, hesitation,
  latency — counters LLM-assisted answering — and produces SEPARATE interview-derived
  scores incl. voice-recording vs live-call comparison metrics (teased in the MVP UI); that full version still needs the «text vs voice
  agent» risk analysis (gaming resistance, deterrence, accessibility, bias, consent). UI
  already carries the teaser note.
- **Self-learning mode for product agents** (operator, Jul 19): agents/assistants keep
  learning MDs — log failures and mistakes, periodically improve their own prompts/behavior
  based on them (same convention as docs/agent-learnings/ for dev agents, but for PRODUCT
  agents, persisted in the DB/repo).
- Real email delivery of follow-up interviews (now mocked).
- MCP server (skill+CLI covers agents in MVP).
- Remaining sourcing channels: LinkedIn, X, ProductHunt, patents (USPTO/EPO), accelerator
  cohorts, Adzuna hiring-velocity, YC OSS API calibration cohort.
- Competitor tech-stack benchmarking via public repos (NotebookLM insight).
- Full deck visual parsing (slide-by-slide claims).
- Adversarial investment committee (multi-agent debate) — stretch if time remains, else here.
- Founder-visible contradiction feedback (fairness loop).
- Founder score prediction intervals (Research Area 1); public-footprint → success study
  (Research Area 3).
- Multi-fund tenancy, per-fund tokens, thesis back-testing.
- Vector DB for semantic search (operator will call it if needed).
