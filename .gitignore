# --- Internal research & intel (never publish) ---
internal/
.claude/
CLAUDE.md
AGENTS.md
.automation/
session-history/

# --- Secrets ---
.env
.env.*
*credentials*
# feature 12 (deploy): real server env files use a *.env suffix, not the .env
# prefix pattern above -- .env.example variants stay tracked (name doesn't match).
infra/vcbrain/*.env

# --- Local junk / runtime ---
.DS_Store
__pycache__/
*.pyc
*.db-wal
*.db-shm
.process-meetings/
node_modules/
other-projects/
infra/supabase/volumes/

# --- Lovable export archives (contents in web/) ---
docs/backlog/08-founder-intake-interview/*.zip

# --- Local junk / test artifacts ---
.playwright-mcp/
/*.png
/ingest_dry_stdout.json
/ingest_dry_stderr.log

# --- Russian doc versions (kept local, not published to public repo) ---
*.ru.md
docs/backlog/_oss-borrow-map.md
