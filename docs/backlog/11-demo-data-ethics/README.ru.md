# 11 · Demo-данные и слой этики

> English version (primary): [README.md](README.md)

Status: **done** — QA-гейт PASSED 19.07.2026, ноль блокеров · Depends on: 02, 08
Closeout: [done.md](done.md) · Карта фикстуры: [fixture-notes.md](fixture-notes.md) · QA: [qa-report-11.md](qa-report-11.md)

## Что уехало (19.07.2026) — читать до дизайн-нарратива ниже

Реально построенное отличается от исходного плана «реальные + синтетика» ниже, и отличие
осознанное:

- **Demo-фикстура — 10 ПОЛНОСТЬЮ синтетических заявок** (`db/fixtures/11-demo-data.sql`), а не
  «3-5 реальных + 1-2 синтетика». Все люди и компании вымышлены, `is_synthetic=true` везде,
  все домены `.example`. 5 inbound (дек) + 5 radar (без дека), каждая построена так, чтобы
  форсировать ровно одно честное UI-состояние (задокументированное противоречие,
  нераскрытый gap, «вне тезиса», forecast-claim, insufficient-evidence, provenance
  «искали — не нашли», HN-only identity, star-farming red flag R2, karma-only obscurity,
  cold-start).
- **Почему полностью синтетика, а не реальные:** фабриковать claims, противоречия или
  red flags *про реального фаундера* — это ровно та клевета, которую предотвращает entity
  gate. Реальные фаундеры в продукте всё же есть: живой корпус ~150 фаундеров с радара
  фичи 02 доступен в дашборде, но им никогда не приписываются выдуманные red flags.
  Поэтому seeded-противоречия для демо живут целиком на синтетических людях.
- **Этические UI-поверхности отдают другие фичи**, по дизайну: бейдж SYNTHETIC и кнопка
  opt-out на карточке фаундера — в фиче 09; заметка «что мы собираем»
  (`DisclosureBanner.tsx`) — в фиче 08. Фича 11 владеет **данными**, **гарантиями read-path**
  (бейдж резолвится для всех 10) и **этическими гарантиями** (erasure работает end-to-end,
  нет полей Art.9 / минимизации, нет ссылок на реальных людей).
- **Сверка сценариев с исходным нарративом** (оба проверены вживую, см. `fixture-notes.md`):
  Kelpgrid попадает «вне тезиса» по **сектору** (climate, вне мандата фонда
  b2b-software/ai-infra/devtools), а не по географии — Дания проходит гео-правило EU/US;
  демо-бит «Outside thesis, никогда rejected» держится в любом случае. Andrei (quietgpu) и
  Tomás (ferrofluid) теперь несут **реальные founder-скоры** (23.68 низкой уверенности /
  71.62 с видимым red flag R2 на оценённой строке), а не схлопываются в insufficient-evidence.

Секции ниже — исходный дизайн/грум-интент, оставлены как контекст.

## Что это

Субстрат для demo и история про compliance: **3-5 РЕАЛЬНЫХ founders**, найденных нашим
собственным radar (свежий HN Show HN / GitHub) + **1-2 синтетических профиля с seeded
противоречиями** (чтобы безопасно продемонстрировать truth-gap и red flags на сцене), плюс
видимый слой этики: только публичные данные, opt-out, data minimisation, уважение robots.txt.

## Зачем (rubric и обоснование)

- Brief §5: датасет не предоставлен — принеси/синтезируй свой; синтетические профили с
  seeded противоречиями предложены явно; **качество ingestion > размер датасета**.
- Судьи в VC-челлендже спросят про легальность (data-sources.md §legal): план — ответить
  ДО того, как спросят — слайд про этику + видимая механика продукта.
- Реальные founders, найденные вживую = самое сильное доказательство sourcing («его нет на
  Crunchbase — мы его нашли»).
- Решение оператора (19 июля): реальные + 1-2 синтетики.

## Откуда идея

- `internal/research/data-sources.md` — полный юридический/этический план: прецедент hiQ с
  обеих сторон, EDPB 03/2026 + чеклист CNIL, data minimisation как одновременно compliance И
  защита от bias, кнопка «право на возражение», прозрачность источников по каждому score.
- Примеры due-diligence-agents (project-atlas) — форма synthetic-with-contradictions.
- RSK-004: синтетики также позволяют честно показать калибровку, не выставляя слабости
  реального человека на сцене (оптика fairness).

## Взгляд на реализацию

1. **Реальные founders**: прогнать radar (02) на свежем окне HN Show HN во время билда;
   отобрать 3-5 с богатыми цифровыми следами (GitHub-хаб резолвится, краулятся личные
   сайты); прогреть scoring заранее, чтобы demo было мгновенным; хранить raw snapshots как
   provenance.
2. **Синтетики**: 1-2 профиля, собранных вручную через небольшой n8n-генератор или руками:
   один «слишком хорош, чтобы быть правдой» (заявляет 10k пользователей, репозиторий создан
   на прошлой неделе из скопированного источника → флаг provenance + противоречие) и один
   честный cold-start (тонкий цифровой след, высокие confidence-gaps → показывает обработку
   отсутствующих данных). Помечены `synthetic=true` в БД, видимо забейджены в UI (никогда не
   представлены как реальные — честность REQ-004).
3. **Слой этики в продукте**: кнопка opt-out на карточке founder'а + публичная заметка «что
   мы собираем» на странице заявки + data minimisation (нигде нет полей возраста/фото/Art.9)
   + проверка robots.txt, видимая в workflow краулинга. Один слайд в demo-видео: «Только
   публичные данные, официальные API, никаких фейковых аккаунтов, никакого обхода CAPTCHA;
   delete-on-request».

## Границы и заглушки

Без бумажной работы по GDPR (demo, заявлено как есть: «для production в EU —
задокументированная оценка legitimate interest по EDPB 03/2026»). Реальные founders: только
данные профессионального сигнала.

## Агенты и режимы работы (оркестрация — читать перед грумом)

- **Сначала план:** @implementation-plan-architect ⇄ @implementation-plan-reviewer (до ✅ APPROVED). Git/деплой — ТОЛЬКО @devops.
- **AI-логика (ОБЯЗАТЕЛЬНО `ai-agent-builder`):** генератор синтетических профилей с зашитыми противоречиями.
- **n8n (ОБЯЗАТЕЛЬНО, два n8n-агента):** pre-warm прогон радара по свежему окну HN (использует workflows фичи 02).
- **Дата-модель:** @database-engineer — флаг `synthetic`; согласовать с 01.
- **UX/Дизайн:** мало, но видимо — бейдж synthetic, кнопка opt-out, заметка «что мы собираем»; @designer вкладывает в 09.
- **QA:** @qa-engineer — синтетика НИКОГДА не рендерится без бейджа (честность REQ-004), opt-out реально удаляет, нет нарушений Art.9/минимизации в собираемых полях.

## Открытые вопросы

- Связаться с реальными founders? (Приятный жест после хакатона: «вас оценило наше demo, вот
  ваша карточка + opt-out» — решить с оператором позже; НЕ во время 24ч.)
