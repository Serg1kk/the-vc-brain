# 07 · Thesis Engine (настраиваемая линза фонда)

> English version (primary): [README.md](README.md)

Статус: **БД + вычислитель ГОТОВЫ** (19.07.2026, QA-гейт по базе пройден) · n8n-воркфлоу в работе ·
Зависит от: 01 · Блокирует: 02 (гейт), 09 (линза)

**Работает уже сейчас, без n8n:** `node lib/f07/run.js <application_id> [--recorded <dir>] [--gate-text <file>]`
прогоняет весь гейт end-to-end против Supabase. Проверено вживую на фикстурах:
Nordkit → `passed` (fit 100), Fogline → `insufficient_evidence` (покрытие 0.38, строка скора
не пишется), GameLoop → `borderline` через мягкий deal-breaker.

Документы: [design.md](design.md) (rev.3a) · [plan.md](plan.md) · [tracker.md](tracker.md) ·
[handoff.md](handoff.md) — **читать, если ведёшь 02, 04, 06, 09 или 10** ·
[qa-report-07.md](qa-report-07.md)

## Что это

Инвестиционный тезис фонда как настраиваемый объект: секторы, стадия, география, размер
чека, целевая доля владения, аппетит к риску. Работает в трёх местах: (a) **pre-filter
gate** на intake (дешёвая первая дверь), (b) **линза на feed** (сортировка/фильтрация по
thesis-fit), (c) **контекст для memo** (рекомендация относительна thesis).

## Зачем (rubric и обоснование)

- MVP-обязательное по брифу §Must-demonstrate 1: «каждая рекомендация отфильтрована и
  оценена через линзу конкретного фонда»; хардкод убивает этот столп (FAQ-15 — явно
  настраиваемо).
- Открытая дверь + pre-filter (REC-003, Carl @47:30): «первая дверь и вторая дверь… нам не
  интересно финансировать очередную ice cream shop». Экономика двухступенчатого screening
  (REC-008): дешёвый текстовый gate → дорогой полный анализ только после gate.
- Их собственный пример: фильтр B2B tech, рейтинг 1-5, 4-5 передаются людям (FACT-001/002).

## Откуда идея

- vcbrain **Thesis Studio** (MIT): must-haves / deal-breakers / взвешенные измерения —
  лучшая OSS-форма этого; адаптировать схему, не код.
- Концепция конфига `firmstyle` из dealflow; текст Sequoia «Services as Software», хранится
  в KB как пример документа thesis для демонстрации thesis-matching.

## Взгляд на реализацию

- Таблица Supabase `thesis`: одна строка на конфиг фонда {sectors[], stages[], geos[],
  check_range, ownership_target, risk_appetite, must_haves[], deal_breakers[], weights_json,
  version}.
- UI (09): простая форма конфига, 6-8 полей + редактируемые чипы; смена thesis
  пересортировывает feed вживую (это wow-момент «линзы» для демо).
- n8n: саб-workflow **`thesis-gate`** (дешёвая LLM-классификация: попадает в thesis?
  сработал deal-breaker? → pass/soft-fail с причиной — soft-fail всё равно сохраняется,
  показывается под «Outside thesis», чтобы открытая дверь оставалась видимой);
  **`thesis-fit-score`** используется ранжированием feed и memo.
- Рекомендация в memo цитирует, какие правила thesis сработали.

## Границы и заглушки

Один фонд (без multi-tenancy). Back-testing thesis (есть у vcbrain) — пометка post-MVP.

## Агенты и режимы работы (оркестрация — читать перед грумом)

- **Сначала план:** @implementation-plan-architect ⇄ @implementation-plan-reviewer (до ✅ APPROVED). Git/деплой — ТОЛЬКО @devops.
- **AI-логика (ОБЯЗАТЕЛЬНО `ai-agent-builder`):** классификатор thesis-gate (дешёвый, in-thesis / deal-breaker с причиной).
- **n8n (ОБЯЗАТЕЛЬНО, два n8n-агента):** `thesis-gate`, `thesis-fit-score`.
- **Дата-модель:** @database-engineer — конфиг-таблица `thesis` (версионируемая); согласовать с 01.
- **UX/Дизайн + UX-брейншторм с оператором:** форма конфига тезиса + интеракция «линза» (переключение тезиса пересортирует фид вживую) — проектирует @designer, собирает @frontend-developer.
- **QA:** @qa-engineer — soft-fail сохраняет open door (gray-out, не скрытие), причины гейта видимы.

## Открытые вопросы — оба ЗАКРЫТЫ

- **Дефолтный demo-thesis** → да, «B2B tech, pre-seed/seed, EU+US, $100K», зеркалит спонсора,
  но со стадией, приведённой к нашей схеме (`pre_seed|seed`; у них late seed → Series A, вне
  скоупа). Засеян с `active=true, is_default=true`. См. design.md §7.
- **Deal-breakers: скрывать или затемнять?** → затемнять, и это уже не склонность, а **D-01** — с
  обоснованием и **принуждением на уровне БД**. `enforcement` задаётся на каждое правило и по
  умолчанию `soft`; `hard` разрешён только для mandate-fatal и фрода, а валидатор отклоняет
  `hard`-правило без корректного `hard_justification` — в том числе когда ключ отсутствует вовсе,
  а именно там наивная plpgsql-проверка молча пропускает.

**Передано дальше:** противоречие «анонимизация против founder-driven оценки» — в фичу 03.

## Post-MVP (сознательно не строим)

Бэктест тезиса · калибровка miss-rate (нужна размеченная когорта, которой у нас нет — показать
непроверенные проценты значило бы сфабриковать метрику) · маскировка личности на входе ·
булева композиция в `expr`.
